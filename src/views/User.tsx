import * as React from 'react';

import { Box, Typography } from '@mui/material';

const User: React.FC<{}> = () => {
  return (
    <div id="user">
      <Box>
        <Typography>
          User
        </Typography>
      </Box>
    </div>
  );
}

export default User;